# TMK SmartBI Export Draft

This folder keeps a historical skill draft that was not yet promoted into the main `skills/` set.

It is kept as a draft because it still depends on:

- live SmartBI access
- an external browser-export script path
- local execution context that is not packaged inside this repository

Promote it only after adding its missing scripts, references, and safety review.
